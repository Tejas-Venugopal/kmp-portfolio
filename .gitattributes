# Normalise all text files to LF in the repo (Windows CRLF on checkout is fine)
* text=auto eol=lf

# Treat binary files as binary — never touch line endings
*.jar    binary
*.wasm   binary
*.png    binary
*.jpg    binary
*.gif    binary
*.ico    binary
*.keystore binary
*.jks    binary

